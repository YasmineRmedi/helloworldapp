pipeline {
   agent any 
   stages {

      stage('Hello') {
       steps {       echo 'Hello World' }

       }
       stage('Java Version') {
       steps {       sh 'cmd /c java -version'}
       }
    }
}
